---
- name: "Initialize laptop from scratch"
  hosts: localhost
  gather_facts: true
  connection: local

  # Ask for sudo password
  vars_prompt:
    - name: "ansible_become_pass"
      prompt: "Sudo password"
      private: yes

  vars_files:
    - vars/main.yml

  roles:
    - { name: ptavares.ansible_role_manage_system, tags: [system, init] }
    - { name: ptavares.ansible_role_oh_my_zsh, tags: [tools, zsh] }
    - { name: vim, tags: [tools, vim] }
    - { name: tmux, tags: [tools, tmux] }
    - { name: gantsign.visual-studio-code, tags: [tools, vsc] }
