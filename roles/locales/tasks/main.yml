---

# Install ntp
- include: ntp.yml
  tags:
    - locales-all
    - locales-ntp

# Init locales
- include: init.yml
  tags:
    - locales-all
    - locales-init

# Configure locales
- include: configure.yml
  tags:
    - locales-all
    - locales-config
